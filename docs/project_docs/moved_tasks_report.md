# Отчёт по перенесённым задачам

Файл автоматически генерируется workflow `.github/workflows/generate-moved-report.yml`.

Дата генерации: 2026-05-25

Описание: в этом файле перечисляются задачи, которые были автоматически перенесены из одной итерации в другую. Для каждой задачи указывается номер, заголовок, текущий статус, итерация, количество переносов и время последнего переноса.

Если файл пустой или содержит только этот текст — workflow ещё не запускался.

Таблица (пример шаблона):

| № | Заголовок | Статус | Итерация | Количество переносов | Последний перенос |
| ---: | --- | --- | --- | ---: | --- |


---

Автоматически сгенерировано/обновлено workflow: `.github/workflows/generate-moved-report.yml`
